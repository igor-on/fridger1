import { GenerateBgColorDirective } from './generate-bg-color.directive';

describe('ColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateBgColorDirective();
    expect(directive).toBeTruthy();
  });
});

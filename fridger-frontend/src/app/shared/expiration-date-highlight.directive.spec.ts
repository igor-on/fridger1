import { ExpirationDateHighlightDirective } from './expiration-date-highlight.directive';

describe('ExpirationDateHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpirationDateHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
